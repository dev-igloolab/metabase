name: Publicar contenedor de IglooLab

on:
  push:
    branches:
      - igloolab
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: igloolab-container
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  METABASE_VERSION: v0.63.18

jobs:
  build-and-push:
    name: Compilar y publicar imagen
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      MB_EDITION: oss
      INTERACTIVE: "false"
      CI: "true"

    steps:
      - name: Descargar el código
        uses: actions/checkout@v6

      - name: Preparar frontend
        uses: ./.github/actions/prepare-frontend
        with:
          node-version: "22"

      - name: Preparar backend
        uses: ./.github/actions/prepare-backend
        with:
          java-version: "25"

      - name: Compilar Metabase
        run: ./bin/build.sh :version "\"${METABASE_VERSION}\""

      - name: Preparar el contexto del contenedor
        run: mv target/uberjar/metabase.jar bin/docker/metabase.jar

      - name: Iniciar sesión en GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Preparar Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Crear y publicar el contenedor
        uses: docker/build-push-action@v7
        with:
          context: bin/docker
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.METABASE_VERSION }}-igloolab
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          build-args: |
            GIT_COMMIT_SHA=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Instalar doctl para el despliegue
        if: ${{ vars.DIGITALOCEAN_APP_ID != '' }}
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Desplegar la nueva imagen en App Platform
        if: ${{ vars.DIGITALOCEAN_APP_ID != '' }}
        run: doctl apps create-deployment "${{ vars.DIGITALOCEAN_APP_ID }}" --force-rebuild
